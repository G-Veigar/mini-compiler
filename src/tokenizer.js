// 词法分析
function tokenizer(codeStr) {
    codeStr = codeStr.trim()
    let codeLength = codeStr.length
    let tokens = []
    for(let i = 0; i < codeLength; i++) {
        let currentLetter = codeStr[i]
    }
}

module.exports = tokenizer